{
  "name": "@designer/core",
  "version": "1.0.0",
  "description": "Data modeling layer for the 3D home designer",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "build:types": "tsc --project tsconfig.build.json --emitDeclarationOnly",
    "prepublishOnly": "npm run build && npm run build:types",
    "publish:dist": "npm publish dist",
    "type-check": "tsc --project tsconfig.build.json --noEmit"
  },
  "peerDependencies": {
    "three": ">=0.170.0"
  },
  "dependencies": {
    "@jscad/modeling": "^2.13.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "three": "^0.170.0",
    "ts-loader": "^9.5.0",
    "typescript": "^5.6.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "MIT"
}
