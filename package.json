{
  "name": "3d-home-design",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace=@designer/editor",
    "build": "npm run build --workspace=@designer/editor",
    "type-check": "tsc --build"
  }
}
